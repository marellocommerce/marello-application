{% block marello_inventory_inventorylevel_widget %}
    <td class="inventorylevel-line-item-warehouse">
        <div class="fields-row">
            {{ form_widget(form.warehouse) }}
        </div>
        {{ form_errors(form.warehouse) }}
    </td>
    <td class="inventory-line-item-adjustment {{ form.quantity.vars.errors|length ? 'has-errors' : '' }}">
        <div class="fields-row">
            {{ form_widget(form.adjustmentOperator) }}
            {{ form_widget(form.quantity) }}
            {{ form_errors(form.quantity) }}
        </div>
    </td>
    <td class="inventorylevel-line-item-purchaselevelqty">
        <div class="fields-row">
            {{ form_widget(form.purchaseInventoryQty) }}
        </div>
        {{ form_errors(form.purchaseInventoryQty) }}
    </td>
    <td class="inventorylevel-line-item-desiredinventoryqty">
        <div class="fields-row">
            {{ form_widget(form.desiredInventoryQty) }}
        </div>
        {{ form_errors(form.desiredInventoryQty) }}
    </td>
{% endblock %}

{% macro marello_inventory_inventorylevel_collection_item_prototype(widget) %}
    {% if 'collection' in widget.vars.block_prefixes %}
        {% set form = widget.vars.prototype %}
        {% set name = widget.vars.full_name ~ '[' ~ widget.vars.prototype.vars.name ~ ']' %}
        {% set disabled = widget.vars.disabled %}
        {% set allow_delete = widget.vars.allow_delete %}
    {% else %}
        {% if widget.vars.disallow_delete is defined and widget.vars.disallow_delete %}
            {% set allow_delete = false %}
        {% else %}
            {% set allow_delete = widget.parent.vars.allow_delete %}
        {% endif %}
        {% set form = widget %}
        {% set name = widget.vars.full_name %}
        {% set disabled = widget.parent.vars.disabled %}
    {% endif %}

    {% set page_component_options = { 'disabled': not allow_delete } %}

    <tr data-content="{{ name }}" data-validation-optional-group {{ block('widget_container_attributes') }}
        class="marello-line-item"
        data-page-component-module="oroui/js/app/components/view-component"
        data-page-component-options="{{ {view: 'marellolayout/js/app/views/abstract-item-view'}|json_encode }}">
        {{ form_widget(form) }}
        {% if allow_delete %}
            <td class="inventorylevel-line-item-remove">
                <button type="button" class="marello-remove-line-item btn icons-holder"><i class="fa-remove"></i></button>
            </td>
        {% elseif widget.parent.vars.allow_delete %}
            <td></td>
        {% endif %}
    </tr>
{% endmacro %}

{% block marello_inventory_inventorylevel_collection_widget %}
    {% spaceless %}
        {% if prototype is defined %}
            {% set prototype_html = _self.marello_inventory_inventorylevel_collection_item_prototype(form) %}
        {% endif %}
        {% set attr = attr|merge({'class': (attr.class is defined ? attr.class ~ ' ' : '') ~ 'marello-item-collection grid-container' }) %}
        {% set id = id ~ '_collection' %}
        <div class="row-oro">
            {% set prototype_name = form.vars.prototype_name %}
            <div {{ block('widget_container_attributes') }}>
                <table class="grid table-hover table table-bordered table-condensed">
                    <thead>
                        <tr>
                            <th class="inventorylevel-line-item-warehouse"><span>{{ 'marello.inventory.inventoryitem.warehouse.label'|trans }}</span></th>
                            <th class="inventorylevel-line-item-inventory-qty"><span>{{ 'marello.inventory.inventorylevel.inventory_qty.label'|trans }}</span></th>
                            <th class="inventorylevel-line-item-purchase-inventory-qty"><span>{{ 'marello.product.purchase_stock_level.label'|trans }}</span></th>
                            <th class="inventorylevel-line-item-desired-inventory-qty"><span>{{ 'marello.product.desired_stock_level.label'|trans }}</span></th>
                            {% if form.vars.allow_delete %}
                                <th class="inventorylevel-row-item-actions"></th>
                            {% endif %}
                        </tr>
                    </thead>
                    <tbody data-last-index="{{ form.children|length}}" data-prototype-name="{{ prototype_name }}"{% if prototype_html is defined %} data-prototype="{{ prototype_html|escape }}"{% endif %}>
                    {% if form.children|length %}
                        {% for child in form.children %}
                            {{ _self.marello_inventory_inventorylevel_collection_item_prototype(child) }}
                        {% endfor %}
                    {% elseif show_form_when_empty and prototype_html is defined %}
                        {{ prototype_html|replace({(prototype_name): '0'})|raw }}
                    {% endif %}
                    </tbody>
                </table>
            </div>

            {% if allow_add %}
                <a class="btn marello-add-line-item" data-container=".marello-item-collection tbody" href="javascript: void(0);"><i class="fa-plus"></i>{{ 'marello.layout.add_entity'|trans({'%entityName%': 'marello.inventory.inventorylevel.entity_label'|trans}) }}</a>
            {% endif %}
        </div>
    {% endspaceless %}
{% endblock %}