{% import 'OroUIBundle::macros.html.twig' as UI %}

{% set route = entity.variant ?
    path('marello_product_add_variant', { parentId: entity.id, id: entity.variant.id, entityClass: oro_class_name(entity, true) })
        : path('marello_product_create_variant' , { id: entity.id })
%}

{{ UI.button({
    'path': route,
    'title': 'marello.product.add_variant.label'|trans,
    'label': 'marello.product.add_variant.label'|trans,
    'iCss': 'fa-barcode',
    'aCss': 'action-link',
}) }}
