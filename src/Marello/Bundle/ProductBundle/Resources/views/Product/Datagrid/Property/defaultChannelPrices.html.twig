{% for index, assembledChannelPriceList in record.getValue('channelPrices') %}
    {% if assembledChannelPriceList.defaultPrice %}
        {{ assembledChannelPriceList.channel.name }}: {{ assembledChannelPriceList.defaultPrice.value|oro_format_currency({'currency': assembledChannelPriceList.currency}) }}<br>
    {% endif %}
{% endfor %}