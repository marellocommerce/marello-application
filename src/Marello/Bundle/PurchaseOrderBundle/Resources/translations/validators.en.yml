marello:
    purchaseorder:
        purchaseorderitem:
            messages:
                error:
                    received_amount: Total amount received can only be as many items as ordered amount.
        due_date:
            messages:
                error:
                    greater_than_date: Due date must be greater than %date%