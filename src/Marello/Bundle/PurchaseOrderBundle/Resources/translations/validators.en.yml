marello:
    purchaseorder:
        purchaseorderitem:
            messages:
                error:
                    received_amount: Total amount received can only be as many items as ordered amount.
        expected_delivery_date:
            messages:
                error:
                    greater_than_date: Expected Delivery date must be greater than today

        purchaseorder:
            due_date:
                type:
                    message: 'This value is not a valid date.'
