{% if is_granted('EDIT', entity) %}
    {% import '@OroUI/macros.html.twig' as UI %}
    {{ UI.button({
        'path' : path('marello_sales_config_saleschannel', { 'id': entity.id }),
        'label': 'oro.config.menu.system_configuration.label'|trans,
        'title': 'marello.sales.config.saleschannel.label'|trans,
        'iCss': 'fa-cog',
    }) }}
{% endif %}
