{% import 'OroUIBundle::macros.html.twig' as UI %}

{% set route = path('marello_inventory_inventory_update', { id: entity.id }) %}

{% if entity.variant %}
{{ UI.button({
    'path': route,
    'title': 'marello.variant_inventory.label'|trans,
    'label': 'marello.variant_inventory.label'|trans,
    'iCss': 'icon-list-alt',
    'aCss': 'action-link',
}) }}
{% endif %}
