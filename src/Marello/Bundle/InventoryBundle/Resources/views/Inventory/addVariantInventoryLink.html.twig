{% import 'OroUIBundle::macros.html.twig' as UI %}

{% set route = path('marello_inventory_inventory_update', { id: entity.id }) %}
{% if entity.variant %}
    {{ UI.button({
        'path': route,
        'title': 'marello.inventory.label'|trans,
        'label': 'marello.inventory.label'|trans,
        'iCss': 'icon-list-alt',
        'aCss': 'action-link',
    }) }}
{% endif %}

