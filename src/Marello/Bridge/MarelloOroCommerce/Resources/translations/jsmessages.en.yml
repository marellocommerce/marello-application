marello:
    orocommerce:
        ups:
            integration:
                deactivate:
                    message: Some shipping methods in <a href="{{ oroLinkGrid }}" target="_blank">the following </a> OroCommerce shipping rules and in <a href="{{ marelloLinkGrid }}" target="_blank">the following </a> Marello shipping rules depend on the integration that is being disabled. The affected shipping methods will be disabled. Shipping rule will also be disabled if none of its shipping methods remain enabled.
                delete:
                    message: Some shipping methods in <a href="{{ oroLinkGrid }}" target="_blank">the following </a> OroCommerce shipping rules and in <a href="{{ marelloLinkGrid }}" target="_blank">the following </a> Marello shipping rules depend on the integration that is being deleted. The affected shipping methods will be disabled. Shipping rule will also be disabled if none of its shipping methods remain enabled.
