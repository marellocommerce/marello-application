{% block marello_servicepoint_business_hours_override_widget %}
    <td class="businesshoursoverrides-line-item-date">
        <div class="fields-row">
            {{ form_widget(form.date) }}
        </div>
        {{ form_errors(form.date) }}
    </td>
    <td class="businesshoursoverrides-line-item-openstatus">
        <div class="fields-row">
            {{ form_widget(form.openStatus) }}
        </div>
        {{ form_errors(form.openStatus) }}
    </td>
    <td class="businesshoursoverrides-line-item-timeperiods">
        <div class="fields-row">
            {{ form_widget(form.timePeriods) }}
        </div>
        {{ form_errors(form.timePeriods) }}
    </td>
{% endblock %}

{% block marello_servicepoint_business_hours_override_collection_widget %}
    {% import _self as fields %}

    {% spaceless %}
        {% if prototype is defined %}
            {% set prototype_html = fields.marello_servicepoint_business_hours_override_collection_prototype(form) %}
        {% endif %}
        {% set attr = attr|merge({'class': (attr.class is defined ? attr.class ~ ' ' : '') ~ 'marello-item-collection grid-container' }) %}
        {% set id = id ~ '_collection' %}
        <div class="row-oro">
            {% set prototype_name = form.vars.prototype_name %}
            <div {{ block('widget_container_attributes') }}>
                <table class="grid table-hover table table-bordered table-condensed">
                    <thead>
                    <tr>
                        <th class="businesshoursoverrides-line-item-dayofweek"><span>{{ 'marello.servicepoint.businesshours.day_of_week.label'|trans }}</span></th>
                        <th class="businesshoursoverrides-line-item-openstatus"><span>{{ 'marello.servicepoint.businesshours.open_status.label'|trans }}</span></th>
                        <th class="businesshoursoverrides-line-item-timeperiods">
                            <div class="businesshoursoverrides-line-item-opentime"><span>{{ 'marello.servicepoint.businesshours.time_periods.label'|trans }}</span></div>
                            <div class="businesshoursoverrides-line-item-closetime"><span>&nbsp;</span></div>
                        </th>
                        {% if form.vars.allow_delete %}
                            <th class="businesshoursoverrides-row-item-actions"></th>
                        {% endif %}
                    </tr>
                    </thead>
                    <tbody data-last-index="{{ form.children|length}}" data-prototype-name="{{ prototype_name }}"{% if prototype_html is defined %} data-prototype="{{ prototype_html|escape }}"{% endif %}>
                    {% if form.children|length %}
                        {% for child in form.children %}
                            {{ fields.marello_servicepoint_business_hours_override_collection_prototype(child) }}
                        {% endfor %}
                    {% elseif show_form_when_empty and prototype_html is defined %}
                        {{ prototype_html|replace({(prototype_name): '0'})|raw }}
                    {% endif %}
                    </tbody>
                </table>
            </div>
            <a class="btn marello-add-line-item" data-container=".marello-item-collection tbody" href="javascript: void(0);"><i class="fa-plus"></i>{{ form.vars.add_label|default('marello.servicepoint.ui.servicepoint_facility.business_hours_override.add.label')|trans }}</a>
        </div>
    {% endspaceless %}
{% endblock %}

{% macro marello_servicepoint_business_hours_override_collection_prototype(widget) %}
    {% if 'collection' in widget.vars.block_prefixes %}
        {% set form = widget.vars.prototype %}
        {% set name = widget.vars.full_name ~ '[' ~ widget.vars.prototype.vars.name ~ ']' %}
        {% set disabled = widget.vars.disabled %}
        {% set allow_delete = widget.vars.allow_delete %}
    {% else %}
        {% if widget.vars.disallow_delete is defined and widget.vars.disallow_delete %}
            {% set allow_delete = false %}
        {% else %}
            {% set allow_delete = widget.parent.vars.allow_delete %}
        {% endif %}
        {% set form = widget %}
        {% set name = widget.vars.full_name %}
        {% set disabled = widget.parent.vars.disabled %}
    {% endif %}
    {% set page_component_options = { 'disabled': not allow_delete } %}

    <tr data-content="{{ name }}"
        class="marello-line-item"
        data-page-component-module="oroui/js/app/components/view-component"
        data-page-component-options="{{ {view: 'marellolayout/js/app/views/abstract-item-view'}|json_encode }}"
        data-layout="separate">
        {{ form_widget(form) }}
        {% if allow_delete %}
            <td class="businesshoursoverrides-line-item-remove marello-line-item-remove">
                <button type="button" class="marello-remove-line-item btn icons-holder"><i class="fa-remove"></i></button>
            </td>
        {% elseif widget.parent.vars.allow_delete %}
            <td></td>
        {% endif %}
    </tr>
{% endmacro %}

{% block marello_servicepoint_business_hours_override_time_period_override_widget %}
    <td class="businesshoursoverrides-line-item-opentime">
        <div class="fields-row">
            {{ form_widget(form.openTime) }}
        </div>
        {{ form_errors(form.openTime) }}
    </td>
    <td class="businesshoursoverrides-line-item-closetime">
        <div class="fields-row">
            {{ form_widget(form.closeTime) }}
        </div>
        {{ form_errors(form.closeTime) }}
    </td>
{% endblock %}

{% block marello_servicepoint_business_hours_override_time_period_override_collection_widget %}
    {% import _self as fields %}

    {% spaceless %}
        {% if prototype is defined %}
            {% set prototype_html = fields.marello_servicepoint_business_hours_override_timeperiod_collection_prototype(form) %}
        {% endif %}
        {% set attr = attr|merge({'class': (attr.class is defined ? attr.class ~ ' ' : '') ~ 'marello-item-collection collection-fields-list' }) %}
        {% set id = id ~ '_collection' %}
        <div class="row-oro">
            {% set prototype_name = form.vars.prototype_name %}
            <div {{ block('widget_container_attributes') }}>
                <table class="grid table-hover table table-bordered table-condensed">
                    <thead>
                    <tr>
                        <th class="businesshoursoverrides-line-item-timeperiod-opentime"><span>{{ 'marello.servicepoint.timeperiod.open_time.label'|trans }}</span></th>
                        <th class="businesshoursoverrides-line-item-timeperiod-closetime"><span>{{ 'marello.servicepoint.timeperiod.close_time.label'|trans }}</span></th>
                        {% if form.vars.allow_delete %}
                            <th class="businesshoursoverrides-row-item-actions"></th>
                        {% endif %}
                    </tr>
                    </thead>
                    <tbody data-last-index="{{ form.children|length}}" data-prototype-name="{{ prototype_name }}"{% if prototype_html is defined %} data-prototype="{{ prototype_html|escape }}"{% endif %}>
                    {% if form.children|length %}
                        {% for child in form.children %}
                            {{ fields.marello_servicepoint_business_hours_override_timeperiod_collection_prototype(child) }}
                        {% endfor %}
                    {% elseif show_form_when_empty and prototype_html is defined %}
                        {{ prototype_html|replace({(prototype_name): '0'})|raw }}
                    {% endif %}
                    </tbody>
                </table>
            </div>
            <a class="btn add-list-item" data-container=".marello-item-collection tbody" href="javascript: void(0);"><i class="fa-plus"></i>{{ form.vars.add_label|default('marello.servicepoint.ui.servicepoint_facility.business_hours.add.label')|trans }}</a>
        </div>
    {% endspaceless %}
{% endblock %}

{% macro marello_servicepoint_business_hours_override_timeperiod_collection_prototype(widget) %}
    {% if 'collection' in widget.vars.block_prefixes %}
        {% set form = widget.vars.prototype %}
        {% set name = widget.vars.prototype.vars.name %}
        {% set disabled = widget.vars.disabled %}
        {% set allow_delete = widget.vars.allow_delete %}
        {% set allow_add_after = widget.vars.allow_add_after %}
    {% else %}
        {% set form = widget %}
        {% set name = widget.vars.full_name %}
        {% set disabled = widget.parent.vars.disabled %}
        {% set allow_delete = widget.parent.vars.allow_delete %}
        {% set allow_add_after = widget.parent.vars.allow_add_after %}
        {% if widget.vars.allow_delete is defined %}
            {% set allow_delete = allow_delete and widget.vars.allow_delete %}
        {% endif %}
    {% endif %}
    <tr data-content="{{ name }}"
        class="marello-line-item"
        data-layout="separate">
        {{ form_widget(form) }}
        {% if allow_delete %}
            <td class="businesshoursoverrides-line-item-remove marello-line-item-remove">
                <button type="button" class="removeRow btn icons-holder"><i class="fa-remove"></i></button>
            </td>
        {% elseif widget.parent.vars.allow_delete %}
            <td></td>
        {% endif %}
    </tr>
{% endmacro %}
